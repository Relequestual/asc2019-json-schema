
<Split style={{ textAlign: 'left', width: "100%" }}>

<CodeSurferLayout style={{ width: '50vw'}}>

<Code
  lang="json"
  code={basicSchemas['001']}
  title="Matchmaker Query Schema"
/>

<Code
  style={{ textAlign: 'left' }}
  lang="json"
  code={basicSchemas['002']}
  title="Matchmaker Query Schema"
  subtitle="notes notes notes"
/>

</CodeSurferLayout>

<div>
  <p>`patient` must have either `genomicFeatures` or `phenotypicFeatures` or both</p>
</div>

<Notes>`patient` must have either `genomicFeatures` or `phenotypicFeatures` or both</Notes>

</Split>
